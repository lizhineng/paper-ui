<button
  type="{{ type }}"
  disabled="{{ disabled }}"
  class="btn btn-{{ size }} {{ text ? 'btn-text' : '' }} {{ pill ? 'btn-pill' : '' }} {{ block ? 'btn-block' : '' }}"
>
  <slot></slot>
</button>